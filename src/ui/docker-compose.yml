version: '3.8'
services:
  ui:
    build: 
      context: .
      dockerfile: frontend/Dockerfile
    restart: always
    ports:
      - 8080:80
    networks:
      - simple_storage
    container_name: ui

networks:
  simple_storage:
    external: true
